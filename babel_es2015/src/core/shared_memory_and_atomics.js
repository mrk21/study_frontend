export default function sharedMemoryAndAtomics() {
  console.group('ES-next: Stage-2: Shared memory and atomics');
  {
    // NOTICE: This code cannot be built at the moment of Dec. 4, 2016.
    /*
    const buffer = new SharedArrayBuffer(1024);
    console.log(buffer);
    */
  }
  console.groupEnd();
}
